import AdminPanel from '../components/AdminPanel';

export const metadata = {
  title: 'Admin Dashboard - VIP Number Station',
};

export default function AdminPage() {
  return (
    <div className="page active">
      <AdminPanel />
    </div>
  );
}
